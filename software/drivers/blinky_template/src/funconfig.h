/**
 * @file funconfig.h
 * @brief ch32v003fun compile-time configuration for the blinky template build.
 *
 * Sets the framework's FUNCONF_* options for this build. The board definition
 * already selects the CH32V003 target and its default 48 MHz HSI+PLL clock, so
 * this template overrides nothing and relies entirely on the framework
 * defaults. See ch32fun.h for the full list of FUNCONF_* options.
 */
#ifndef _FUNCONFIG_H
#define _FUNCONFIG_H

#endif
